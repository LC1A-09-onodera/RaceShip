#include "MeshCollider.h"
#include "CollisionPrimitive.h"

using namespace DirectX;

void MeshCollider::ConstructTriangles(Model* model)
{
	//triangles.clear();
	//int start = 0;
	//for (int i = 0; i < ; i++)
	//{
	//	size_t triangleNum = model->mesh.indices.size() / 3;

	//}
}